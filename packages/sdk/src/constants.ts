/** Protocol version */
export const HMP_VERSION = "1.0";

/** Minimum payment amount (USDC) */
export const MIN_PAYMENT = 0.1;

/** Maximum payment amount (USDC) */
export const MAX_PAYMENT = 5;

/** Timestamp validity window (ms): 5 minutes */
export const TIMESTAMP_WINDOW_MS = 5 * 60 * 1000;

/** Default request timeout (ms): 30 seconds */
export const DEFAULT_TIMEOUT_MS = 30 * 1000;

/** Solana network */
export const SOLANA_NETWORK = "solana:mainnet-beta";

/** Payment token */
export const PAYMENT_TOKEN = "USDC";
